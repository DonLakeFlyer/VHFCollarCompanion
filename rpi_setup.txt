Editor - nano
ps aux - list processes
sudo kill ### - kill process
sudo if config - show ip address

# GNU Radio Install

sudo apt-get install gnuradio

# Custom AirSpy GNU Radio plugin

https://osmocom.org/projects/gr-osmosdr/wiki

sudo apt-get install cmake libairspy0 libairspy-dev swig

cd ~/repos
git clone https://github.com/DonLakeFlyer/gr-airspysdr.git
cd gr-airspysdr
mkdir build
cd build/
cmake ../ -DENABLE_RTL=OFF -DENABLE_REDPITAYA=OFF -DENABLE_RFSPACE=OFF -DENABLE_RTL_TCP=OFF -DENABLE_FCD=OFF -DENABLE_FCDPP=OFF
make
sudo make install
sudo ldconfig

# rPi service setup
cp vhf.service /lib/systemd/system
cp grc.service /lib/systemd/system
sudo chmod 644 /lib/systemd/system/vhf.service
sudo chmod 644 /lib/systemd/system/grc.service
sudo systemctl daemon-reload
sudo systemctl enable vhf.service
sudo systemctl enable grc.service

# LCD setup - config.txt file on SD Card

max_usb_current=1
hdmi_group=2
hdmi_mode=1
hdmi_mode=87
hdmi_cvt 800 480 60 6 0 0 0

# SITL testing

make posix gazebo_typhoon_h480
python3 main.py --sitl True

Modify posix-configs/SITL/init/ekf2/typhoon_h480
mavlink start -x -u 14557 -r 4000000 -m onboard -o 14540 -f

# Turn on UART?

enable_uart=1

# Service install

